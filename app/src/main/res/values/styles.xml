<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColorSecondary">@color/turquoise</item>
    </style>


    <!-- Default text font -->
    <style name="DefaultTextStyle">
        <item name="android:textColor">#000000</item>
        <item name="android:textSize">11sp</item>
        <item name="android:fontFamily">@font/robotoregular</item>
    </style>

    <!-- Font titre -->
    <style name="TitleTextStyle" parent="DefaultTextStyle">
        <item name="android:textSize">22sp</item>
    </style>

    <!-- Font sous titre -->
    <style name="SubtitleTextStyle" parent="DefaultTextStyle">
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">@font/robotomedium</item>
    </style>

    <!-- Font texte bouton -->
    <style name="ButonTextStyle" parent="">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">14sp</item>
        <item name="android:fontFamily">@font/robotoblack</item>
    </style>

    <!-- Font send mail form title -->
    <style name="MailFormTextStyle" parent="TitleTextStyle">
        <item name="android:textColor">#888888</item>
    </style>

    <!-- Style bottom nav bar -->
    <style name="BottomNavigationStyle" parent="Widget.Design.BottomNavigationView">
        <item name="android:textSize">14dp</item>
        <item name="android:fontFamily">@font/robotoregular</item>

    </style>

    <!-- Popup -->
    <style name="WideDialog" parent="Base.Theme.AppCompat.Dialog">
        <item name="android:windowMinWidthMajor">100%</item>
        <item name="android:windowMinWidthMinor">100%</item>
    </style>

    





</resources>
